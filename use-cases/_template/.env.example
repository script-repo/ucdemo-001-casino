# Variables specific to __TITLE__.
# Shared-resource variables live in the repo root .env.example — do not repeat
# them here.

# Base URL of this use case's backend, if it has one.
__ENV_PREFIX___API_URL=http://localhost:8100

# Vector collection this use case owns. Must be prefixed with the slug.
__ENV_PREFIX___COLLECTION=__SLUG____default
