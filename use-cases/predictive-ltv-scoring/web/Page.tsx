import { LtvApp } from "./LtvApp";

/**
 * Expected Player Value (Predictive LTV Scoring).
 * Import only from react, next/*, and this folder.
 */
export default function Page() {
  return <LtvApp />;
}
