apiVersion: v1
kind: Service
metadata:
  name: casino-ai-portal
  labels:
    app.kubernetes.io/name: casino-ai-portal
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: casino-ai-portal
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30007
